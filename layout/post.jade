extends _layouts/base

include _partials/helpers

block title
  title= `${page.title} | ${config.title}`

block content

  .container-introduction
    +siteTitle(config)

  .container-posts
    +post(page, config, true)
